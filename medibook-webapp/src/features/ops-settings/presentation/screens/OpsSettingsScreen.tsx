import { Card } from '@/shared/ui/Card';

/** Placeholder — the ops-settings feature agent ports the real screen next (spec §6). */
export function OpsSettingsScreen() {
  return <Card>{'Ops Settings — being ported'}</Card>;
}
