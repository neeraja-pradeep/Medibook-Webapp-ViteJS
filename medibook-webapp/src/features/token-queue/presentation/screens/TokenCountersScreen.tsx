import { Card } from '@/shared/ui/Card';

/** Placeholder — the token-queue feature agent ports the real screen next (spec §6). */
export function TokenCountersScreen() {
  return <Card>{'Token Counters — being ported'}</Card>;
}
