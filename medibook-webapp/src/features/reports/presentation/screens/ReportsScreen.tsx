import { Card } from '@/shared/ui/Card';

/** Placeholder — the reports feature agent ports the real screen next (spec §6). */
export function ReportsScreen() {
  return <Card>{'Reports — being ported'}</Card>;
}
