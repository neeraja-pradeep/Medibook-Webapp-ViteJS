import { Card } from '@/shared/ui/Card';

/** Placeholder — the dashboard feature agent ports the real screen next (spec §6). */
export function AdminDashboardScreen() {
  return <Card>{'Admin Dashboard — being ported'}</Card>;
}
