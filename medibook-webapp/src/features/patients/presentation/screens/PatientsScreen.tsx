import { Card } from '@/shared/ui/Card';

/** Placeholder — the patients feature agent ports the real screen next (spec §6). */
export function PatientsScreen() {
  return <Card>{'Patients — being ported'}</Card>;
}
