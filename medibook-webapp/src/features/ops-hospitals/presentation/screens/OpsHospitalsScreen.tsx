import { Card } from '@/shared/ui/Card';

/** Placeholder — the ops-hospitals feature agent ports the real screen next (spec §6). */
export function OpsHospitalsScreen() {
  return <Card>{'Ops Hospitals — being ported'}</Card>;
}
