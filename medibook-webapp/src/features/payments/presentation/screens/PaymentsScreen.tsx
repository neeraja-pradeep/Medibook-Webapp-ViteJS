import { Card } from '@/shared/ui/Card';

/** Placeholder — the payments feature agent ports the real screen next (spec §6). */
export function PaymentsScreen() {
  return <Card>{'Payments — being ported'}</Card>;
}
