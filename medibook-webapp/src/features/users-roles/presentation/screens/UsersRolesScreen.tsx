import { Card } from '@/shared/ui/Card';

/** Placeholder — the users-roles feature agent ports the real screen next (spec §6). */
export function UsersRolesScreen() {
  return <Card>{'Users & Roles — being ported'}</Card>;
}
