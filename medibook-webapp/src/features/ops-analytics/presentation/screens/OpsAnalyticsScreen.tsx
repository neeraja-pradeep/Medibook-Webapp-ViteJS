import { Card } from '@/shared/ui/Card';

/** Placeholder — the ops-analytics feature agent ports the real screen next (spec §6). */
export function OpsAnalyticsScreen() {
  return <Card>{'Ops Analytics — being ported'}</Card>;
}
