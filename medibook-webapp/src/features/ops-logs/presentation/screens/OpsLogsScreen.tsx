import { Card } from '@/shared/ui/Card';

/** Placeholder — the ops-logs feature agent ports the real screen next (spec §6). */
export function OpsLogsScreen() {
  return <Card>{'Ops Logs — being ported'}</Card>;
}
