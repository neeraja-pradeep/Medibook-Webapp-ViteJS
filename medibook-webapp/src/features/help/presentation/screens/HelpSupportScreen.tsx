import { Card } from '@/shared/ui/Card';

/** Placeholder — the help feature agent ports the real screen next (spec §6). */
export function HelpSupportScreen() {
  return <Card>{'Help & Support — being ported'}</Card>;
}
