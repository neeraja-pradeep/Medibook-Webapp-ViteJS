import { Card } from '@/shared/ui/Card';

/** Placeholder — the ops-dashboard feature agent ports the real screen next (spec §6). */
export function OpsDashboardScreen() {
  return <Card>{'Ops Dashboard — being ported'}</Card>;
}
