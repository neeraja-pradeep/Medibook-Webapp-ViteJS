import { Card } from '@/shared/ui/Card';

/** Placeholder — the ops-plans feature agent ports the real screen next (spec §6). */
export function OpsPlansScreen() {
  return <Card>{'Ops Plans — being ported'}</Card>;
}
