import { Card } from '@/shared/ui/Card';

/** Placeholder — the settings feature agent ports the real screen next (spec §6). */
export function HospitalSettingsScreen() {
  return <Card>{'Hospital Settings — being ported'}</Card>;
}
