import { Card } from '@/shared/ui/Card';

/** Placeholder — the ops-settlements feature agent ports the real screen next (spec §6). */
export function OpsSettlementsScreen() {
  return <Card>{'Ops Settlements — being ported'}</Card>;
}
