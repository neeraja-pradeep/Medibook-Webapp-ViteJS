import { Card } from '@/shared/ui/Card';

/** Placeholder — the ops-platform-users feature agent ports the real screen next (spec §6). */
export function OpsPlatformUserDetailScreen() {
  return <Card>{'Ops Platform User Detail — being ported'}</Card>;
}
