import { Card } from '@/shared/ui/Card';

/** Placeholder — the ops-reports feature agent ports the real screen next (spec §6). */
export function OpsReportsScreen() {
  return <Card>{'Ops Reports — being ported'}</Card>;
}
