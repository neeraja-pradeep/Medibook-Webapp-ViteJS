import { Card } from '@/shared/ui/Card';

/** Placeholder — the settlements feature agent ports the real screen next (spec §6). */
export function SettlementsScreen() {
  return <Card>{'Settlements — being ported'}</Card>;
}
