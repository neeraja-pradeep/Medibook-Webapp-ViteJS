import { Card } from '@/shared/ui/Card';

/** Placeholder — the ops-notifications feature agent ports the real screen next (spec §6). */
export function OpsNotificationsScreen() {
  return <Card>{'Ops Notifications — being ported'}</Card>;
}
