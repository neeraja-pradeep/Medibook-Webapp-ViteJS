import { Card } from '@/shared/ui/Card';

/** Placeholder — the appointments feature agent ports the real screen next (spec §6). */
export function CreateAppointmentScreen() {
  return <Card>{'Create Appointment — being ported'}</Card>;
}
