import { Card } from '@/shared/ui/Card';

/** Placeholder — the ops-billing feature agent ports the real screen next (spec §6). */
export function OpsInvoiceDetailScreen() {
  return <Card>{'Ops Invoice Detail — being ported'}</Card>;
}
