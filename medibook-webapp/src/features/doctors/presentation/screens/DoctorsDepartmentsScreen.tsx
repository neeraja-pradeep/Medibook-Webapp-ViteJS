import { Card } from '@/shared/ui/Card';

/** Placeholder — the doctors feature agent ports the real screen next (spec §6). */
export function DoctorsDepartmentsScreen() {
  return <Card>{'Doctors & Departments — being ported'}</Card>;
}
