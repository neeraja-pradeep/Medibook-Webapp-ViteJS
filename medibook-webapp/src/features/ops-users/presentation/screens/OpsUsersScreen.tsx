import { Card } from '@/shared/ui/Card';

/** Placeholder — the ops-users feature agent ports the real screen next (spec §6). */
export function OpsUsersScreen() {
  return <Card>{'Ops Users — being ported'}</Card>;
}
